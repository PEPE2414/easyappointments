# Ensure npm uses correct registry and settings
registry=https://registry.npmjs.org/
legacy-peer-deps=false

# For Railway deployments, allow scripts to run
ignore-scripts=false

# Use npm install behavior even with package-lock.json
package-lock=true
